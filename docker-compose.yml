version: '3.8'

services:
  bot:
    build: .
    env_file:
      - .env
    restart: always
  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_PASSWORD=example
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
